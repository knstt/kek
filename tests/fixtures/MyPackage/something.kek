// This file belongs to the MyPackage package because it lives in the
// tests/fixtures/MyPackage/ directory.
//
// All .kek files in the same directory are part of the same package namespace.
// Code outside this directory can import the package with:
//
// #import(tests/fixtures/MyPackage);
//
// and then call this function as:
//
// MyPackage::MyPackageFunc();

// Declarations are exported by default until visibility rules are added.
i32:MyPackageFunc() {
	return(69);
}

T:PkgGenericEcho<T>(T:value) {
	return(value);
}
